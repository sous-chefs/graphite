#!/bin/sh

type="<%= @options[:type] %>"
instance="<%= @options[:instance] %>"
name="${type}-${instance}"
storage_path="<%= node['graphite']['storage_dir'] %>"
pid="$storage_path/${name}.pid"

if [ -e $pid ]; then
    rm -v $pid
fi
